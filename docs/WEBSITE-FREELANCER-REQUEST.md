# Quote Request — WordPress/WooCommerce Website Build

*Send this to 3+ developers.*

---

Hi, I need a quote for building a WordPress + WooCommerce homepage/landing site for a custom furniture business. This is for the **full website build only** — a separate, already-live coming-soon placeholder exists and is out of scope here.

**Confirmed stack (please quote against this exact stack, not your preferred alternative):**
- **CMS:** WordPress
- **E-commerce:** WooCommerce
- **Page builder:** Kadence Blocks + Kadence theme — **free tier only**, no Kadence Pro, no Elementor Pro, no other premium/subscription page builder. This is a hard requirement, not a preference — the client wants zero ongoing software subscription costs.
- **SEO:** Yoast SEO (free)
- **Custom code:** Code Snippets plugin for any PHP outside `functions.php`; Kadence blocks support Custom CSS directly; native Gutenberg has a Custom HTML block for any JS/HTML injection needed.
- **Multilingual:** Polylang or WPML (your call on which — note which one you'd use and why) — see the bilingual section below for exactly how it's scoped.

Please confirm in your reply that you're comfortable building in **Kadence Blocks specifically** (not just "a WordPress page builder" generically) — its scroll-reveal animation system is a specific part of the scope (see below).

**Site structure:**
One primary homepage/landing page (single scrolling page, not a multi-page marketing site). The one product in the catalog (a customizable Bed Frame) is presented at a standard catalog level in this quote — see the exclusion note below, this is important.

**What's included:**

**1. Homepage build in Kadence Blocks**, matching the existing brand identity already established on the coming-soon page: black (`#000000`) background, brand accent color `#996633`, Cormorant Garamond (English serif) + Cairo (Arabic) + Inter (detail/UI) fonts, subtle grain texture. I'll provide the logo files and brand assets.

**2. Hero: custom Q&A catchphrase slideshow.** This is a **custom-built component**, not a Kadence-native feature — a smoother, more polished version of the auto-cycling ticker already previewed on the coming-soon page. Bilingual, auto-cycling, direction-aware: English lines slide in from the right, Arabic lines slide in from the left, no user controls. The final list of Q&A catchphrase pairs will be provided separately — price the *component* (reusable, loops through any number of phrase pairs), not a fixed phrase count.

**3. Rest of the homepage: light fade-in/fade-out only.** Every section below the hero uses Kadence's native scroll-reveal block animations (included free, no plugin) — simple fade-in as the section enters the viewport, fade-out as it leaves. No slider/carousel plugin is needed anywhere on this site.

**4. WooCommerce catalog with one product listed** (Bed Frame) — standard product page: title, images, base price, add-to-cart. **Do NOT price or scope any custom conditional configurator logic for this product** (position/insertion/size fields, conditional dependencies, file upload, etc.) — that is a separate, larger piece of work being quoted independently once a plugin decision is finalized. If you want to also quote for that separately, let me know and I'll send the full spec, but keep it as a distinct number in your reply, not bundled into this quote.

**5. Yoast SEO setup** — basic on-page SEO configuration for the homepage and the product page.

**6. Bilingual architecture (confirmed, two different approaches for two different parts of the site):**

- **Homepage:** simultaneous English + Arabic, side by side, **no language switcher, no multilingual plugin** — same custom technique already used on the coming-soon page (both languages visible together as content pairs), just built out further for a fuller page.
- **WooCommerce's own pages (cart, checkout, my-account):** these are dynamically generated by WooCommerce itself and can't render two languages simultaneously on one page — a multilingual plugin is required here specifically. Configure it to **silently auto-detect the visitor's browser language on first visit** (reading the standard browser `Accept-Language` setting) and serve that language for the rest of the session — **no visible language switcher UI anywhere on the site**. If the browser's language is neither English nor Arabic, default to English.

Please flag in your reply if you see any technical issue with keeping the homepage plugin-free while scoping the multilingual plugin narrowly to WooCommerce's own pages — this is the architecture we want, but we're open to hearing if you'd approach it differently.

**What's excluded from this quote:**
- Bed Frame Product Configurator conditional logic (separate quote, see above)
- Hosting and domain registration/migration (handled separately)
- Any plugin licensing costs (client purchases these directly, not part of your invoice)
- The coming-soon placeholder page (already built, live)
- Ongoing content updates, maintenance, or support after delivery

**Please include in your reply:**
1. A fixed total price for the build described above (not hourly)
2. Expected delivery time
3. Confirmation you build specifically in Kadence Blocks (not just "a page builder")
4. Confirmation you've built bilingual (English/Arabic RTL) WordPress/WooCommerce sites before
5. Which multilingual plugin you'd use for the WooCommerce pages (Polylang or WPML) and why
6. Confirmation you understand the homepage is custom-coded bilingual (no plugin) while only the WooCommerce pages use the multilingual plugin

Thanks!
