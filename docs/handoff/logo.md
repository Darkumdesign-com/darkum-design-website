# Logo, ✅ Complete & Delivered

- Final files exported to: `G:\My Drive\OneDriveBackup\Designs\Darkum Design - Creativity\Logo\Darkum Design - Logo Exports` (path corrected 2026-08-17, this doc previously listed a stale/incorrect path).
- **Format folders:** PNG, JPG, WebP, SVG, EPS, AI
- **Color variants:** Black, White, Transparent
- **Layout variants:** Circular, Exact, Favicon, Horizontal
- **Sizes:** 512px, 512px-PNG8 (PNG only), 1024px, 2048px, 300ppi (raster) / Vector (SVG, EPS, AI)
- **Total:** 192 files (finalized 2026-08-17 after cleanup, see the full logo-mistake/re-export/cleanup story below, was 176 before the master fix, briefly 216 mid-re-export before duplicates were removed), naming convention verified consistent (`Darkum-Design-Logo-[Color]-[Layout]-[Size].[ext]`)
- Delivered to partners via WhatsApp with a plain-language explainer covering formats/colors/sizes/use cases (Arabic version offered, not yet sent). **Superseded 2026-08-17, see below**, going forward, partners get the whole export folder via Google Drive instead of further WhatsApp batches.
- **Note:** EPS/AI "Transparent" files don't carry a true alpha channel in EPS (AI is fine, native format), worth mentioning if a print vendor asks for a "transparent EPS."
- **Fixed (2026-08-12):** "Exact" layout PNG/JPG/WebP files were mislabeled, suffixes claimed square sizes (`512px`, `1024px`, `2048px`) but the actual images are non-square (`Exact` preserves the logo's natural proportions, ~410:489 ratio, unlike the square-cropped `Circular`/`Favicon` layouts). Pixel content was correct; only filenames were wrong. Renamed 27 files across PNG/JPG/WebP to reflect true dimensions (e.g. `-512px` → `-410x489px`, `-1024px` → `-820x978px`, `-2048px` → `-1640x1954px`). No re-export needed, `300ppi` suffix was unaffected since it describes resolution, not claimed pixel size. **Already-sent WhatsApp files to partners were not affected/reissued**, only the local export folder was corrected.
- **Logo mistake found and fixed (2026-08-17):** Sobhy caught and corrected an error in the logo itself (design-level, not just a filename issue like the 2026-08-12 fix above). **Corrected "Exact" layout base size is 417x502px (Black), 417x503px (White), 418x503px (Transparent)**, tiny per-color variance is expected and consistent with the pre-fix behavior, not a bug. Superseded the earlier ~410:489 figure and the filenames derived from it. **Full re-export and rename completed 2026-08-17:** all 176 original files replaced by a fresh 216-file export (added a `192px` size across the board plus a `PDF` vector format for partner-friendly viewing), old export folder preserved as a dated reference rather than deleted (`Darkum Design - Logo Exports - OLD (pre-master-fix 2026-08-17)`). Two issues caught mid-process and both resolved same day: (1) the Transparent color variant's Exact artboard hadn't received the master fix and was still exporting at the old proportions, fixed by Sobhy, reverified; (2) the JPG `192px` export row was mislabeled, actually exporting at 2x/1024px-equivalent scale, row removed (192px favicon need is covered by the PNG version instead, which was already correctly sized), stale mislabeled JPG files moved to a `_stale-192px-mislabeled` subfolder rather than deleted. Final rename pass (30 files, PNG/JPG/WebP "Exact" layout) confirmed via `magick identify` and applied cleanly, no collisions. True dimensions now baked directly into every "Exact" filename: `-192x232px`, `-417x502px`/`-417x503px`/`-418x503px` (color-dependent), `-834x1004px`, `-1668x2008px`/`-1667x2008px`, plus `-300ppi` unchanged.
- **Final cleanup and verification, completed 2026-08-17:** two more issues caught and resolved after the rename above. (1) Sobhy raised whether the 12 JPG Transparent files should exist at all, since JPG has no alpha channel, confirmed via pixel comparison (`magick compare -metric AE`) that they differ from the equivalent White JPGs by well under 1.5% of pixels (anti-aliasing rounding from the alpha-to-white flatten, not a real visual difference), and confirmed the *original* 2026-08-12 export never had a Transparent JPG folder at all, only Black/White, so this was JPG's correct, established behavior all along, not a new decision. The 12 Transparent JPGs were moved out (not deleted). (2) Sobhy separately suspected all 12 EPS files (and, by extension, the 12 AI files) were duplicates of one combined document, since EPS doesn't support per-artboard export. **Checked and disproven:** SHA-256 hashes are unique across all 12 EPS files and all 12 AI files, and file sizes cluster distinctly by layout (Horizontal consistently largest, Exact consistently smallest) rather than clustering uniformly as a bundled document would, Sobhy independently confirmed this by opening files directly in Illustrator, which showed one clean single artboard per file; the earlier impression came from Windows Explorer's own EPS thumbnail preview handler showing a misleading generic multi-logo composite, not real file content. No EPS/AI files were touched. **Final folder state:** 192 files across 7 format folders (PNG 72, JPG 24, WebP 48, SVG 12, EPS 12, AI 12, PDF 12), all correctly named, no stray/duplicate/mislabeled files remaining inside the shareable folder. Two "reference only, not for sharing" folders live one level up, siblings of the export folder rather than inside it: `Darkum Design - Logo Exports - OLD (pre-master-fix 2026-08-17)` and `_duplicate-JPG-Transparent-no-alpha-2026-08-17`.
- **Delivery method changed 2026-08-17, WhatsApp reissue question resolved:** rather than reissuing individual files over WhatsApp a third time, Sobhy already told partners about the earlier mistakes directly, and partners explicitly said they'd prefer being able to browse/download from one place instead of receiving batches of files ("at least we can open Google Drive and download or share one specific file, not all these confusing files"). **Decision: share the whole `Darkum Design - Logo Exports` folder with partners via Google Drive** going forward, instead of further WhatsApp exports. **Not yet actioned**, the folder itself is confirmed ready/clean (see above), but Sobhy has not yet actually shared it with partners. Tracked as an open item, see `resolved-items-log.md`.
- **Brand primary color confirmed:** the mark's native fill is `#996633` (hex shorthand `#963` in the SVG source), this is the single source of truth for the brand's primary color, now also the site's `--brand-primary-color` CSS variable (see `repo-infra-coming-soon-page.md`).
- **Pricing/market-research history:** logo pricing was validated against real Khamsat freelancer quotes ($40–80) before the item-by-item proposal was superseded by Bido's flat-rate offer, full detail archived to `docs/offer_market-research.md`.
