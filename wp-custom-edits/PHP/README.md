GoDaddy Pro Code Snippets, PHP mirror. Each file here is a 1:1 copy of a snippet pasted into the Code Snippets plugin (wp-admin > Snippets). See `../Docs/wp-reference.md` for how this folder fits into the overall setup, and each file's own docblock for its specific execution scope ("Run everywhere" vs "Only run on Site Front-end") and activation notes.

- `enable-svg.php`, allows SVG uploads through the media library.
- `floating-whatsapp-button.php`, sitewide fixed WhatsApp float button, `wp_footer`.
- `jsonld-furniturestore.php`, FurnitureStore JSON-LD structured data, `wp_head`, sitewide.
